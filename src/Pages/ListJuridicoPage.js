import { LitElement, html } from 'lit-element';

class ListJuridicoPage extends LitElement {
    render() {
        return html`<h1>Juridico</h1>`;
    }
}

window.customElements.define('list-juridico-page',ListJuridicoPage)
